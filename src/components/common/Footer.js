import React, {Proptypes} from 'react';
// import { Link, IndexLink } from 'react-router';

const Footer = () => {
  return(
    <footer>
      &copy;2017
    </footer>
  );
};

export default Footer;
